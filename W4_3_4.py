nombre_entier = 19.97
nombre_int = int(nombre_entier) #float() funktioniert gleich, setzt aber der Zahl (z.B. 30) einfach eine .0 an, also 30.0. Int() rundet nicht, sondern entfernt nur die Kommastellen

print(nombre_entier, "change à ", nombre_int);